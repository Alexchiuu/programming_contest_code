#include<bits/stdc++.h>
#define int long long

using namespace std;

signed main(){

    int n;
    cin >> n;

    int p;
    cin >> p;

    vector<vector<int> > g(n);

    while(p --){
        int a, b;
        cin >> a >> b;
        
    }

    return 0;
}