#include <bits/stdc++.h> 
#define fastio ios_base::sync_with_stdio(0);cin.tie(0);

using namespace std;

int main(){

    fastio

    long long x, y;
    cin >> x >> y;
    cout << x + y - 2 << '\n';

    return 0;    
}